#ifndef __AUTO_TILE_PANEL_H__
#define __AUTO_TILE_PANEL_H__

#include <element.h>

class AutoTilePanel{
    public:
        AutoTilePanel() = default;
        AutoTilePanel(std::string _name, Base_Editor* editor);
        ~AutoTilePanel() = default;
        
};

#endif